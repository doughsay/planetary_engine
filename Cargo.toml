[package]
name = "planetary_engine"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.18.0"
big_space = { version = "0.12.0", features = ["i64"] }
noise = "0.9.0"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
